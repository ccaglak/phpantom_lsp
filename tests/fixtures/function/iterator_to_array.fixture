// test: iterator_to_array on typed iterator resolves to array of item type
// feature: completion
// Adapted from phpactor function/iterator_to_array.test

// expect: fooMethod(
---
<?php

class Foo {
    public function fooMethod(): void {}
}

/** @var \Iterator<int, Foo> $iter */

$items = iterator_to_array($iter);
$items[0]-><>