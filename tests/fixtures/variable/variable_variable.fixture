// test: variable-variable ${$bar} resolution
// feature: completion
// ignore: Variable-variable resolution is not supported
// Adapted from phpactor variable/braced_expression.test
// expect: getName(
---
<?php

class User {
    public function getName(): string { return ''; }
}

function test(): void {
    $bar = 'user';
    $$bar = new User();
    ${$bar}-><>
}