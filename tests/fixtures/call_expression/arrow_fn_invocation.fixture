// test: invoked arrow function return type resolves for completion
// feature: completion
// Adapted from phpactor call-expression/type-from-invoked-callable.test
// expect: format(
---
<?php

class DateTime {
    public function format(string $f): string {}
}

$result = (fn (): DateTime => new DateTime())();
$result-><>